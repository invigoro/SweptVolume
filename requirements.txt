# Top-level dependencies for the swept-volume assignment (GPU track).
#
# PyTorch must come from the CUDA 12.9 wheel index; the +cu129 local version
# tag below exists only there. Install with:
#
#   pip install -r requirements.txt
#
# The --extra-index-url directive on the next line is honoured by pip, so no
# additional command-line flags are required.
--extra-index-url https://download.pytorch.org/whl/cu129

torch==2.13.0+cu129
numpy==2.5.3
matplotlib==3.11.2

# Required by the course computing handout; not used by the neural-network
# experiments themselves, but the same environment is reused in later projects.
swig==4.5.0
gymnasium[atari,box2d,classic-control,other]==1.3.0
